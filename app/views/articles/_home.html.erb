
  Dibuat oleh <%#= @articles.author_name %> pada <%#= @articles.created_at.strftime('%d %b, %Y')%></br>
  <%= link_to 'Edit', edit_article_path(@articles) %> || <%= link_to 'Hapus', article_path(@articles), method: :delete, data: {confirm: 'Hapus Artikel Ini?'} %></br>
