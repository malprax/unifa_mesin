<h1>Halaman Index</h1>

<table>
  <tr>
    <th>Judul</th>
    <th>Isi</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 
  <% @pages.each do |page| %>
  <tr>
    <td><%= page.judul %></td>
    <td><%= page.isi %></td>
    <td><%= link_to 'Show', page_path(page) %></td>
    <td><%= link_to 'Edit', edit_page_path(page) %></td>
    <td><%= link_to 'Destroy', page_path(page),
                    method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= link_to 'buat baru', new_page_path %>