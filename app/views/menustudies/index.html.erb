<h1>Listing menustudies</h1>

<table>
  <tr>
    <th>Kode</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @menustudies.each do |menustudy| %>
  <tr>
    <td><%= menustudy.kode_id %></td>
    <td><%= link_to 'Show', menustudy %></td>
    <td><%= link_to 'Edit', edit_menustudy_path(menustudy) %></td>
    <td><%= link_to 'Destroy', menustudy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menustudy', new_menustudy_path %>
